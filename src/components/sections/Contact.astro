---
import { Button } from "@/components/ui/button.tsx";
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
} from "@/components/ui/card.tsx";
import { Input } from "@/components/ui/input.tsx";
import { Textarea } from "@/components/ui/textarea.tsx";
import { Phone, Mail, MapPin, CheckCircle2 } from "lucide-react";
import { COMPANY } from "@/config";
---

<section id="contact" class="py-24 sm:py-32 bg-white">
  <div class="container mx-auto px-4">
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h2 class="text-3xl md:text-4xl font-extrabold text-chambray-900">
        Contacta con Nosotros
      </h2>
      <p class="mt-4 text-lg text-chambray-600">
        ¿Tienes un proyecto en mente o una urgencia? Estamos aquí para ayudarte.
        Rellena el formulario o utiliza nuestros otros canales de contacto.
      </p>
    </div>

    <!-- Grid Layout -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
      <!-- Left Column: Contact Info & Map -->
      <div class="space-y-8">
        <div>
          <h3 class="text-2xl font-bold text-chambray-800">
            Información de Contacto
          </h3>
          <p class="mt-2 text-chambray-600">
            Atendemos en toda la Comunidad de Madrid.
          </p>
        </div>

        <div
          class="space-y-6 text-chambray-700"
          itemscope
          itemtype="http://schema.org/LocalBusiness"
        >
          <meta itemprop="name" content={COMPANY.name} />
          <meta itemprop="image" content="/logo.png" />
          <!-- Asegúrate de tener un logo.png en tu carpeta public -->
          <div class="flex items-start gap-4">
            <Phone className="w-6 h-6 text-chambray-600 mt-1 flex-shrink-0" />
            <div>
              <h4 class="font-semibold">Teléfono</h4>
              <a
                href={`tel:${COMPANY.phone}`}
                itemprop="telephone"
                class="hover:text-chambray-900 transition-colors"
                >{COMPANY.phone_display}</a
              >
            </div>
          </div>
          <div class="flex items-start gap-4">
            <Mail className="w-6 h-6 text-chambray-600 mt-1 flex-shrink-0" />
            <div>
              <h4 class="font-semibold">Email</h4>
              <a
                href={`mailto:${COMPANY.email}`}
                itemprop="email"
                class="hover:text-chambray-900 hover:underline transition-colors"
                >{COMPANY.email}</a
              >
            </div>
          </div>
          <div
            class="flex items-start gap-4"
            itemprop="address"
            itemscope
            itemtype="http://schema.org/PostalAddress"
          >
            <MapPin className="w-6 h-6 text-chambray-600 mt-1 flex-shrink-0" />
            <div>
              <h4 class="font-semibold">Ubicación</h4>
              <p>
                <span itemprop="streetAddress">{COMPANY.street}</span>,
                <span itemprop="postalCode">{COMPANY.postal_code}</span>,
                <span itemprop="addressLocality"
                  >{COMPANY.city}, {COMPANY.region}</span
                >,
                <span itemprop="addressCountry">{COMPANY.country}</span>
              </p>
            </div>
          </div>
        </div>

        <!-- Google Maps Iframe -->
        <div
          class="aspect-w-16 aspect-h-9 rounded-lg overflow-hidden border-2 border-chambray-200"
        >
          <iframe
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d380.24537048041793!2d-3.480945514935301!3d40.32097950000812!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0xd423ebf71863be3%3A0x1e5036b70fe18ce9!2sLa%20Azucarera%2C%20C.%20de%20las%20Hilanderas%2C%2031%2C%2028500%20Arganda%20del%20Rey%2C%20Madrid!5e0!3m2!1ses!2ses!4v1759858102878!5m2!1ses!2ses"
            width="100%"
            height="400"
            style="border:0;"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            title="Ubicación en Google Maps"></iframe>
        </div>
      </div>

      <!-- Right Column: Contact Form -->
      <Card className="bg-chambray-50 border-0 shadow-lg flex flex-col">
        <CardHeader>
          <CardTitle className="text-3xl text-chambray-900"
            >Envíanos un mensaje</CardTitle
          >
        </CardHeader>
        <CardContent id="form-container" className="flex-grow flex">
          <!-- Form -->
          <form id="contact-form" class="space-y-4 flex flex-col w-full">
            <div class="space-y-1">
              <label for="name" class="text-sm font-medium text-chambray-700"
                >Tu Nombre <span class="text-red-500">*</span></label
              >
              <Input id="name" type="text" name="name" required />
            </div>
            <div class="space-y-1">
              <label for="email" class="text-sm font-medium text-chambray-700"
                >Tu Email <span class="text-red-500">*</span></label
              >
              <Input id="email" type="email" name="email" required />
            </div>
            <div class="space-y-1">
              <label for="phone" class="text-sm font-medium text-chambray-700"
                >Tu Teléfono <span class="text-red-500">*</span></label
              >
              <Input
                id="phone"
                type="tel"
                name="phone"
                pattern="[0-9\s+]{9,15}"
                title="Por favor, introduce un número de teléfono válido (9-15 dígitos)."
                required
              />
            </div>
            <div class="space-y-1">
              <label for="subject" class="text-sm font-medium text-chambray-700"
                >Asunto</label
              >
              <Input id="subject" type="text" name="subject" />
            </div>
            <div class="space-y-1 flex-grow flex flex-col">
              <label for="message" class="text-sm font-medium text-chambray-700"
                >Tu Mensaje <span class="text-red-500">*</span></label
              >
              <Textarea
                id="message"
                name="message"
                className="flex-grow"
                required
              />
            </div>
            <div class="space-y-1">
              <div class="flex items-start space-x-2">
                <input
                  type="checkbox"
                  id="privacy"
                  name="privacy"
                  required
                  class="mt-1"
                />
                <label for="privacy" class="text-sm text-chambray-600">
                  He leído y acepto la <a
                    href="/politica-de-privacidad"
                    target="_blank"
                    class="underline hover:text-chambray-900"
                    >Política de Privacidad</a
                  >. <span class="text-red-500">*</span>
                </label>
              </div>
            </div>
            <Button
              type="submit"
              size="lg"
              className="w-full bg-chambray-600 hover:bg-chambray-700 text-white"
            >
              Enviar Mensaje
            </Button>
          </form>
          <!-- Confirmation Message -->
          <div id="confirmation-message" class="hidden w-full">
            <div
              class="flex flex-col items-center justify-center text-center h-full w-full"
            >
              <CheckCircle2 className="w-16 h-16 text-green-600 mb-4" />
              <h3 class="text-2xl font-bold text-chambray-900">
                ¡Mensaje Enviado!
              </h3>
              <p class="mt-2 text-chambray-600">
                Gracias por contactarnos. Te responderemos lo antes posible.
              </p>
            </div>
          </div>
        </CardContent>
      </Card>
    </div>
  </div>

  <script>
    const form = document.getElementById("contact-form");
    const confirmationMessage = document.getElementById("confirmation-message");

    if (form instanceof HTMLFormElement && confirmationMessage) {
      form.addEventListener("submit", (event) => {
        event.preventDefault(); // Evita el envío y recarga de la página

        // Oculta el formulario y muestra el mensaje de confirmación
        form.style.display = "none";
        confirmationMessage.style.display = "flex";

        // Después de 10 segundos, oculta el mensaje y muestra el formulario de nuevo
        setTimeout(() => {
          confirmationMessage.style.display = "none";
          form.style.display = "flex";
          form.reset(); // Limpia los campos del formulario
        }, 10000);
      });
    }
    // 5000 milisegundos = 5 segundos);
  </script>
</section>
